#include <stdio.h>
#include <stdlib.h>
/**
 * main - prints the alphabet in lowercase
 * Return: 0 (Success)
 */
int main(void)
{
	int char c
	
	for (c = 'a'; ch <= 'z'; c++);
	putchar(c);
	putchar('\n');
	return (0);
}
